{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "React Dev Server",
      "type": "shell",
      "isBackground": true,
      "problemMatcher": ["$eslint-stylish"],
      "command": "yarn --cwd ${workspaceFolder}/web && yarn --cwd ${workspaceFolder}/web start",
      "options": { "env": { "BROWSER": "none" } }
    }
  ]
}
